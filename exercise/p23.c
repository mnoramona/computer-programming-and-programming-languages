#include <stdio.h>
#include <math.h>
void cos(int a, int b, int c){
    double cosA, cosB, cosC;
    return cosA = (c*c + b*b - a*a)/(2*b*c);
    return cosB = (a*a + c*c - b*b)/(2*a*c);
    return cosC = (a*a + b*b - c*c)/(2*a*b);
}
void unghi(int )
int main() 
{
    int x, y, z;
    scanf("%d %d %d", x, y, z);
    
    return 0;
}